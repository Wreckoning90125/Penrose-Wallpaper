<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Preference
        android:key="nav_back"
        android:title="← Back"
        android:summary="" />

    <PreferenceCategory android:title="Projection">
        <ListPreference
            android:key="projection"
            android:title="Mode"
            android:summary="Euclidean (default; affine view) or Poincaré disk (the Circle Limit projection)"
            android:entries="@array/projection_entries"
            android:entryValues="@array/projection_values"
            android:defaultValue="0" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Poincaré disk">
        <SeekBarPreference
            android:key="hyp_scale"
            android:title="Boundary compression"
            android:summary="How strongly the projection piles up tiles against the disk boundary. Low → an even radial squeeze, tiles fill the screen, disk boundary off-screen. High → far tiles compress against the boundary and the disk edge becomes visible — the Circle Limit feel. The view auto-fits the projected tiling to the screen, so the slider only controls compression, never visibility."
            android:min="0" android:max="100" android:defaultValue="50"
            app:showSeekBarValue="true" />
        <SeekBarPreference
            android:key="hyp_boost_x"
            android:title="Disk centre — X"
            android:summary="Hyperbolic translation along the X axis. 50 = centred at the origin; 0/100 shifts the disk centre to b ≈ (-0.9, 0) / (+0.9, 0)."
            android:min="0" android:max="100" android:defaultValue="50"
            app:showSeekBarValue="true" />
        <SeekBarPreference
            android:key="hyp_boost_y"
            android:title="Disk centre — Y"
            android:summary="Hyperbolic translation along the Y axis. 50 = centred at the origin."
            android:min="0" android:max="100" android:defaultValue="50"
            app:showSeekBarValue="true" />
        <SeekBarPreference
            android:key="hyp_edge_subdiv"
            android:title="Edge polyline subdivision"
            android:summary="Each tile edge is split into this many sub-segments before projection. 1 = straight chord (cheapest); 16 reads as a true geodesic arc at typical zoom. Triggers a geometry rebuild."
            android:min="1" android:max="32" android:defaultValue="1"
            app:showSeekBarValue="true" />
    </PreferenceCategory>
</PreferenceScreen>
